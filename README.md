# fauxfetus

1. `audio` must exist as a root-level symlink to faux mp3 archive
1. TODO: During deployment, `audio` must exist as a root-level directory containing the faux mp3 archive
1. The faux mp3 archive directory must follow this structure: `artist-slug/album-slug/track-slug.mp3`
